spring:
  view:
    prefix: /WEB-INF/view/
    suffix: .jsp
  profiles:
    active: a
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test
    username: root
    password: mysql

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080